{
    "name": "images-api",
    "version": "1.0.0",
    "description": "API to handle the storing and retrieving of images",
    "author": "lamberjack",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "start:dev": "NODE_ENV=dev node server.js",
        "test": "NODE_ENV=test mocha  --exit --recursive -timeout 8000",
        "load:dev": "NODE_ENV=dev node loader.js",
        "load:test": "NODE_ENV=test node loader.js",
        "init:dev": "NODE_ENV=dev node initializer.js",
        "init:test": "NODE_ENV=test node initializer.js"
    },
    "dependencies": {
        "body-parser": "^1.20.0",
        "cors": "^2.8.5",
        "dotenv": "^16.0.1",
        "express": "^4.18.1",
        "mariadb": "^3.0.0",
        "multer": "^1.4.5-lts.1",
        "mysql": "^2.18.1",
        "rxjs": "^7.5.5",
        "sequelize": "^6.20.1"
    },
    "devDependencies": {
        "chai": "^4.3.6",
        "mocha": "^10.0.0",
        "supertest": "^6.2.3"
    }
}
